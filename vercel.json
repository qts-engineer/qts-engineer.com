{
  "buildCommand": "NODE_OPTIONS=--openssl-legacy-provider SKIP_PRERENDER=true npm run build",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "framework": "create-react-app",
  "env": {
    "NODE_OPTIONS": "--openssl-legacy-provider",
    "SKIP_PRERENDER": "true"
  }
}

