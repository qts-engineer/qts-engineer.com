{
  "buildCommand": "NODE_OPTIONS=--openssl-legacy-provider SKIP_PRERENDER=true VERCEL=1 npx react-scripts build && echo 'Build complete - postbuild skipped'",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "framework": "create-react-app",
  "env": {
    "NODE_OPTIONS": "--openssl-legacy-provider",
    "SKIP_PRERENDER": "true",
    "VERCEL": "1"
  }
}

