{
  "buildCommand": "NODE_OPTIONS=--openssl-legacy-provider SKIP_PRERENDER=true VERCEL=1 npm run build",
  "outputDirectory": "build",
  "installCommand": "npm install",
  "env": {
    "NODE_OPTIONS": "--openssl-legacy-provider",
    "SKIP_PRERENDER": "true",
    "VERCEL": "1"
  }
}

