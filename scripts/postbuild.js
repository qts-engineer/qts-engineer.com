#!/usr/bin/env node

// DEFINITIVE FIX: Completely disable react-snap to prevent Puppeteer errors
// This script does nothing - react-snap will never run

console.log('postbuild: Skipped (react-snap disabled)');
process.exit(0);

